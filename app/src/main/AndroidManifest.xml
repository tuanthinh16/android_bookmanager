<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muontrasach">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Return_BookActivity"></activity>
        <activity
            android:name=".Account_ManagerActivity"
            android:label="@string/app_account" />
        <activity
            android:name=".Borrow_ManagerActivity"
            android:label="@string/app_borrow" />
        <activity
            android:name=".AddBookActivity"
            android:label="@string/app_addbook" />
        <activity
            android:name=".BookManagerActivity"
            android:label="@string/book_mana" />
        <activity
            android:name=".HomePageActivity"
            android:label="@string/app_admin_manager" />
        <activity
            android:name=".CreateAccountActivity"
            android:label="@string/app_create" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>